feature missing:
	parse the juniper "unicast statement in BGP"
	parse the juniper configuration taking care of protocol bgp and group export/configuration

bug:
	the rendering does not like larger/smaller values of size_y (svg most affected)
	ipv6 matching for exchange test (may work need to test)

idea:
	look at inlining the svg:
		http://wiki.svg.org/Inline_SVG
		http://code.google.com/p/svgweb/
		<script type="image/svg+xml">
			<svg>
			</svg>
		</script>
	filter route from Linx RS when you have a direct peering connection.
	integration with asused
